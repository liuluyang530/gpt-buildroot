diff -Nur uboot-tools-2018.03/common/image.c uboot-tools-2018.03-back/common/image.c
--- uboot-tools-2018.03/common/image.c	2018-03-13 20:02:19.000000000 +0800
+++ uboot-tools-2018.03-back/common/image.c	2019-09-16 20:08:47.404798971 +0800
@@ -86,6 +86,7 @@
 	{	IH_ARCH_ARC,		"arc",		"ARC",		},
 	{	IH_ARCH_X86_64,		"x86_64",	"AMD x86_64",	},
 	{	IH_ARCH_XTENSA,		"xtensa",	"Xtensa",	},
+	{	IH_ARCH_GPT,		"gpt",		"gpt ",   },
 	{	-1,			"",		"",		},
 };
 
@@ -335,9 +336,13 @@
 	image_print_type(hdr);
 	printf("%sData Size:    ", p);
 	genimg_print_size(image_get_data_size(hdr));
+#if defined(CONFIG_ARCH_GPT)
+	printf("%sLoad Address: %08lx\n", p, (ulong)image_get_load(hdr));
+	printf("%sEntry Point:  %08lx\n", p, (ulong)image_get_ep(hdr));
+#else
 	printf("%sLoad Address: %08x\n", p, image_get_load(hdr));
 	printf("%sEntry Point:  %08x\n", p, image_get_ep(hdr));
-
+#endif
 	if (image_check_type(hdr, IH_TYPE_MULTI) ||
 			image_check_type(hdr, IH_TYPE_SCRIPT)) {
 		int i;
diff -Nur uboot-tools-2018.03/include/image.h uboot-tools-2018.03-back/include/image.h
--- uboot-tools-2018.03/include/image.h	2018-03-13 20:02:19.000000000 +0800
+++ uboot-tools-2018.03-back/include/image.h	2019-09-16 20:04:40.891642717 +0800
@@ -19,6 +19,8 @@
 #include "compiler.h"
 #include <asm/byteorder.h>
 
+//#include "configs/polaris_fpga.h"
+
 /* Define this to avoid #ifdefs later on */
 struct lmb;
 
@@ -191,6 +193,7 @@
 	IH_ARCH_X86_64,			/* AMD x86_64, Intel and Via */
 	IH_ARCH_XTENSA,			/* Xtensa	*/
 	IH_ARCH_RISCV,			/* RISC-V */
+	IH_ARCH_GPT,                    /*GPT           */
 
 	IH_ARCH_COUNT,
 };
@@ -308,8 +311,13 @@
 	__be32		ih_hcrc;	/* Image Header CRC Checksum	*/
 	__be32		ih_time;	/* Image Creation Timestamp	*/
 	__be32		ih_size;	/* Image Data Size		*/
+#if defined(CONFIG_ARCH_GPT)
+	__be64		ih_load;	/* Data	 Load  Address		*/
+	__be64		ih_ep;		/* Entry Point Address		*/
+#else
 	__be32		ih_load;	/* Data	 Load  Address		*/
 	__be32		ih_ep;		/* Entry Point Address		*/
+#endif
 	__be32		ih_dcrc;	/* Image Data CRC Checksum	*/
 	uint8_t		ih_os;		/* Operating System		*/
 	uint8_t		ih_arch;	/* CPU architecture		*/
@@ -716,8 +724,13 @@
 image_get_hdr_l(hcrc)		/* image_get_hcrc */
 image_get_hdr_l(time)		/* image_get_time */
 image_get_hdr_l(size)		/* image_get_size */
+#if defined(CONFIG_ARCH_GPT)
+image_get_hdr_ll(load)		/* image_get_load */
+image_get_hdr_ll(ep)		/* image_get_ep */
+#else
 image_get_hdr_l(load)		/* image_get_load */
 image_get_hdr_l(ep)		/* image_get_ep */
+#endif
 image_get_hdr_l(dcrc)		/* image_get_dcrc */
 
 #define image_get_hdr_b(f) \
@@ -774,8 +787,13 @@
 image_set_hdr_l(hcrc)		/* image_set_hcrc */
 image_set_hdr_l(time)		/* image_set_time */
 image_set_hdr_l(size)		/* image_set_size */
+#if defined(CONFIG_ARCH_GPT)
+image_set_hdr_ll(load)		/* image_set_load */
+image_set_hdr_ll(ep)		/* image_set_ep */
+#else
 image_set_hdr_l(load)		/* image_set_load */
 image_set_hdr_l(ep)		/* image_set_ep */
+#endif
 image_set_hdr_l(dcrc)		/* image_set_dcrc */
 
 #define image_set_hdr_b(f) \
